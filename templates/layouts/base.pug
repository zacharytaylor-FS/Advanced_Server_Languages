doctype html
html(lang='en')
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible", content="IE=edge")
    meta(name="viewport", content="width=device-width, initial-scale=1.1")
    title
    script(src="https://kit.fontawesome.com/afc2c54677.js" crossorigin="anonymous") 
    link(type='text/css' href='/stylesheets/style.css' rel='stylesheet')
  body
    block header
      include ../partials/_header.pug
  block content
    const variants, products
  block footer
    include ../partials/_footer.pug
  
    //- div(class='container-fluid')
    //-   p This is a line #[em some emphasis] and #[strong strong text] markup.


    //- <!-- You can add HTML comments directly -->
    //- // You can add single line JavaScript comments and they are generated to HTML comments
    //- //- Introducing a single line JavaScript comment with "//-" ensures the comment isn't rendered to HTML

    //- p A line with a link
    //-   a(href='/catalog/authors') 
    //-   |  and some extra text.

    //- #container.col
    //-   if product
    //-     p A variable named "product" exists.
    //-   else
    //-     p A variable named "product" does not exist.
    //-   p.
    //-     Pug is a terse and simple template language with a
    //-     strong focus on performance and powerful features.

    //- h2 Generate a list
    //- ul
    //-   each product in ['Nike Shoe','Adidas']
    //-     li= product
